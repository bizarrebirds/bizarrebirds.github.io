# [Humanities for STEM Brains](https://stembrainstutor.com/)

Install [pnpm](https://pnpm.io/installation), then install and run with:
```bash
pnpm install
npm run dev
```

Only the last line is required after the initial installation.

Deploy by pushing to the `main` branch.